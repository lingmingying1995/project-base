# 跨电脑待办 · project-base 开源补齐

> 创建于工作电脑（Mac），2026-08-09
> 明天在另一台电脑上核查后继续

---

## 背景

今天在工作电脑上做 project-base 开源准备，发现 **8月7日的commit（09c3b6b）把文档升级到了"完整版目标"，但部分文件没实际加进去就push了**。

工作电脑上已经做了以下改动（未提交）：
- 新增 LICENSE（MIT）
- 新增 .github/ISSUE_TEMPLATE/（bug-report.yml + rule-suggestion.yml）
- README.md 加了"30秒看懂"章节
- 从 workbench 拷了3个缺失agent：project-director.md / tech-director.md / task-guardian.md
- 从 workbench 拷了2个缺失规则：Skill管理规则.md / 项目索引规则.md

**这些改动还没提交。明天在另一台电脑上 git pull 之前，先看本文件。**

---

## 待核查项（明天在另一台电脑上做）

### 核查1：提示词手册.md 到底存不存在

凌坚持说自己创建过"提示词手册.md"。但今天在工作电脑上核查：
- project-base 的 git 全历史里没有这个文件（`git log --all --oneline -- "提示词手册.md"` 无输出）
- project-base 的远程 origin/main 也没有（`git ls-tree -r origin/main --name-only | grep 提示词` 无输出）
- workbench 也没有这个文件

**明天要做的**：
1. 在另一台电脑上搜索 `提示词手册.md`（全局搜索，不只是 project-base 目录）
2. 检查是不是只在工作电脑的某个未提交的本地文件里（`git status` / `git stash list`）
3. 检查是不是在 raisenote 或其他项目里
4. 如果找到了 → 拷到 project-base 根目录，补提交
5. 如果确实没有 → 从 README/操作手册/AGENTS.md 里删掉对"提示词手册.md"的引用，开源后再补

**核查后的结论记录到这里**：
- [x] 找到了，在 Windows 工作电脑（CHINAMI-NJDMT06）的 project-base 根目录，一直是 untracked 状态从未提交，所以 Mac 上 pull 不到。已于 2026-08-10 补提交（commit 67e3c79）

### 核查2：scripts 脚本模板要不要现在补

scripts/README.md 描述了完整脚本清单（sync.bat/sync.sh/auto-summary.*/task-guardian-*.bat/schtasks-xml/plist），但实际只有 README.md 和 ocr-image.py。

workbench 有这些脚本，但写死了"workbench""工作台"等字样，模板化需要逐行改。

**明天要做的**：
1. 确认另一台电脑上 project-base 的 scripts/ 目录内容是否跟工作电脑一致（可能那边有未提交的脚本文件）
2. 如果那边也缺 → 决定：现在模板化补齐，还是先开源核心（agent+规则）后续补脚本
3. 如果选"先开源后续补" → 需要把 scripts/README.md 和 README/操作手册里对脚本的描述改成"即将提供"，避免文档说有但实际没有

**核查后的结论记录到这里**：
- [x] Windows 工作电脑上有完整的 scripts/（untracked），已补提交（commit 67e3c79）。同时发现 Windows 上缺 sync.bat 实例版和 write-task-log.ps1，已新建并配置 ProjectBaseSync 计划任务（详见下方 2026-08-10 补充）

### 核查3：8月7日那次commit到底在哪儿做的

commit 09c3b6b 的作者是 lingmingying1995，日期 8月7日（周五）。凌说"7号是最后工作的时间"。

**明天要做的**：
1. `git log --format="%h %ai %s" 09c3b6b -1` 看具体是哪台电脑、什么时间提交的
2. 如果是在另一台电脑上做的 → 检查那台电脑上有没有未提交的文件（`git status` / `git stash list`）
3. 那次commit可能是在那台电脑上"写了文档但文件没加完就push了"，另一台电脑上可能有未提交的工作进度

**核查后的结论**：
09c3b6b 是 8月7日在 Mac 工作电脑上提交的。Windows 工作电脑上当时有大量未提交改动（提示词手册、scripts、模板化 agent），这些改动从未提交所以 Mac 那边看不到。根因是 **project-base 在 Windows 上没有配 sync.bat 和计划任务**（workbench 配了，project-base 漏了），导致改动一直堆在本地。已于 2026-08-10 补齐 sync 机制并提交所有改动。

---

## 2026-08-10 补充（Windows 工作电脑核查）

### 核查结论汇总

| 核查项 | 结论 |
|--------|------|
| 核查1：提示词手册 | ✅ 找到了，在 Windows 工作电脑 project-base 根目录，untracked 从未提交。已补提交 |
| 核查2：scripts | ✅ Windows 上有完整 scripts/（untracked）。已补提交 + 新建 sync.bat 实例版 + write-task-log.ps1 + ProjectBaseSync 计划任务 |
| 核查3：8月7日 commit | 09c3b6b 在 Mac 上提交。Windows 上有大量未提交改动，根因是 project-base 没配 Windows 同步机制 |

### 新增的 Windows 同步机制

**根因**：project-base 在 Windows 上没有 sync.bat 和计划任务（workbench 有，project-base 漏了），导致本地改动永远不会自动提交。

**已修复**：
1. 新建 `scripts/write-task-log.ps1`（通用任务日志写入器，独立于 workbench）
2. 新建根目录 `sync.bat`（project-base 实例版，PROJECT_NAME=ProjectBase）
3. 新建 `ProjectBaseSync` 计划任务（每天 18:50 触发）
4. 把 sync.bat 模板版（scripts/sync.bat）也补了任务日志写入逻辑

**sync.bat 踩的坑**（已全部修复）：
- 嵌套 if 块在 stash 后中断脚本 → 改 goto 扁平结构
- sync.bat 本身是 untracked，被 `git stash -u` 带走 → 提交后变 tracked 解决
- write-task-log.ps1 目录名拼错（"产出成果"→"产出"）→ 修正 char 码
- `:pushonly` 标签冲突，fall-through 到 `:pullfail` → 重构 push 流程
- schtasks /tr 路径含空格被截断 → 用 `\"` 转义引号

### Mac 那边需要做的

Windows 上的 sync.bat 修复（goto 扁平结构、push 流程重构）还没同步到 `scripts/sync.bat` 模板。Mac 下次 pull 后，如果要用 scripts/sync.bat 模板，记得也把这些修复同步过去。模板同步可以后续做，不紧急。

---

## 已完成的改动（工作电脑，未提交）

| 改动 | 文件 | 状态 |
|---|---|---|
| 新增MIT协议 | LICENSE | ✅ 完成 |
| 新增issue模板 | .github/ISSUE_TEMPLATE/bug-report.yml | ✅ 完成 |
| 新增issue模板 | .github/ISSUE_TEMPLATE/rule-suggestion.yml | ✅ 完成 |
| README加30秒版 | README.md | ✅ 完成 |
| 补齐缺失agent | agents/project-director.md | ✅ 从workbench拷 |
| 补齐缺失agent | agents/tech-director.md | ✅ 从workbench拷 |
| 补齐缺失agent | agents/task-guardian.md | ✅ 从workbench拷 |
| 补齐缺失规则 | 规则/Skill管理规则.md | ✅ 从workbench拷 |
| 补齐缺失规则 | 规则/项目索引规则.md | ✅ 从workbench拷 |

**注意**：从 workbench 拷过来的文件内容是 workbench 版本，可能包含 workbench 专属内容。明天在另一台电脑上核查时，要检查这些文件有没有需要去 workbench 化的地方（比如文件里提到"工作台"的地方）。

---

## 补齐完成后的开源前检查清单

核查1、2、3都完成后，开源前还要做：

- [ ] 确认 README 目录树跟实际目录完全一致（数字、文件名都对上）
- [ ] 确认操作手册.md 跟实际目录一致
- [ ] 确认 AGENTS.md 目录树跟实际一致
- [ ] 确认 git clone 地址正确（remote 是 `project--base` 双连字符，README 写的是单连字符，要统一）
- [ ] 在 GitHub 上把仓库从 private 改成 public
- [ ] 提交所有改动并 push
- [ ] 在抖音/小红书发"开源了"的内容

---

## 今天对话的关键决策（备查）

1. **开源时机**：不等"完美"再开源，能对外本身就是打磨过程
2. **开源协议**：MIT（最宽松，最大化传播）
3. **开源顺序**：地基包先开，raisenote等上线+闸门方案落地后再开
4. **宣传口径**：raisenote不是"免费使用"，是"免费开始用，重度付费，企业咨询"
5. **认知沉淀**：写到了 成长日志/自我认知/开源不是发电机是放大器-2026-08-09.md
6. **灵感稿**：写到了 视频稿件/创作方案/视频灵感-我为什么把地基包开源了.md

---

*跨电脑待办 · 2026.08.09 · 工作电脑*
