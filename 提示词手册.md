# 提示词手册 · 对 AI 说什么能干什么

> 本文档给用户看。列出你能用 AI 干什么，以及对应说什么话触发。
> 不用记，用的时候翻一下就行。AI 会读这个文件，你说关键词它就知道该做什么。
>
> 创建日期：2026-08-04

---

## 功能目录

你想做什么，翻到对应章节：

| 你想……                   | 翻到第几章                       |
| ---------------------- | --------------------------- |
| 从地基包建一个新项目             | [[#一、建新项目\|一、建新项目]]         |
| 提需求 / 加功能 / 改 bug / 重构 | [[#二、开发功能\|二、开发功能]]         |
| 提交代码 / 同步到 GitHub      | [[#三、提交和同步代码\|三、提交和同步代码]]   |
| 生成每日总结                 | [[#四、每日总结\|四、每日总结]]         |
| 换电脑 / 新电脑配环境 / 两台电脑同步  | [[#五、多电脑和换电脑\|五、多电脑和换电脑]]   |
| 加规则 / 定规矩 / 改文档        | [[#六、维护规则和文档\|六、维护规则和文档]]   |
| 做个 skill / 固化流程        | [[#七、Skill\|七、Skill]]       |
| 查项目状态 / 看改了什么          | [[#八、查状态\|八、查状态]]           |
| 手动触发某个 Agent 帮忙检查      | [[#九、Agent 协作\|九、Agent 协作]] |
| 做个 Agent / 固化协作角色      | [[#十、Agent\|十、Agent]]       |

---

## 一、建新项目

**场景**：clone 了地基包，要开始一个新项目。

| 你说什么 | AI 做什么 |
|---------|----------|
| "读操作手册，开始初始化项目" | 读 `操作手册.md`，引导你完成6步初始化（项目信息→填文件→配Git→配Agent→配定时任务→验证） |

只在 clone 地基包后第一次用。初始化完成后不用再说，AI 每次开工自动读 AGENTS.md。

---

## 二、开发功能

**场景**：想加功能、改 bug、重构代码。

### 提需求 / 加功能

| 你说什么 | AI 做什么 |
|---------|----------|
| "做个XX功能" / "加个XX" / "改一下XX" | 触发**项目总监**，由项目总监分发：先调需求拆解员拆成改动清单，列关联模块和风险点，等你确认后再动手 |
| "拆解一下" / "先别急着改" | 手动触发需求拆解员（跳过项目总监，直接拆解） |
| "走流程" / "按流程来" | 手动触发项目总监，走完整开发流程 |
| "做吧" / "开始吧" / "就这样" | 确认需求，AI 开始写代码 |
| "你看着办" | AI 给2-3个方案让你选，**不会**自己替你定（技术选型尤其如此） |

**关键**：你说需求 ≠ 确认要做。AI 会先讨论、给方案，你说"做吧"才动手。想跳过讨论直接写代码，明确说"做吧，按X方案"。

### 改 bug

| 你说什么 | AI 做什么 |
|---------|----------|
| "XX报错了" / "修一下XX" | AI 先定位问题，给修复方案，确认后改（小bug可能直接改） |
| "这个bug怎么回事" | 只分析不动手，告诉你原因 |

### 重构

| 你说什么 | AI 做什么 |
|---------|----------|
| "评估一下架构" / "这个功能会不会影响结构" | 触发**架构师**，评估改动对整体结构的影响，预警耦合和风险 |
| "重构一下XX" | 触发架构师评估 → 拆解员拆清单 → 你确认后再改 |

---

## 三、提交和同步代码

**场景**：改完代码要提交、要推送到 GitHub。

| 你说什么 | AI 做什么 |
|---------|----------|
| "提交" / "push" / "同步一下" | **commit + push 一气呵成**，不会提交完停下来问"要推送吗" |
| "好了" / "先这样" / "下次聊" | 对话收尾，AI 自动检查有没有未提交的改动，有就提交+推送 |

**例外**（这些情况 AI 会问一次）：
- 第一次推送到新远程仓库
- 需要 force push
- 当前分支没有上游
- push 被拒（远端有新提交）—— 告诉你原因，问"要不要 pull 后重试"

详细规则见 `规则/Git同步规则.md`。

---

## 四、每日总结

**场景**：想自动归档今天的对话内容，生成每日总结。

| 你说什么 | AI 做什么 |
|---------|----------|
| "生成今天的每日总结" / "跑一下每日总结" | 手动跑 `scripts/auto-summary.js`，读今天的对话 → 调 GLM 分析 → 写总结文件 |
| "补跑X号的总结" | 用 `--date=YYYY-MM-DD` 参数补跑指定日期的总结 |

**归档位置**：`产出/每日总结/YYYY-MM/每日总结-YYYY-MM-DD.md`（按月份子目录归档，当天文件可多次追加）。

**自动归档**：配了定时任务的话（见[[#五、多电脑和换电脑|五、多电脑和换电脑]]），每天自动跑，不用手动触发。

**多电脑**：家用电脑用 `auto-summary.bat home`，生成的文件带 `-HOME` 后缀，不会和工作电脑冲突。

---

## 五、多电脑和换电脑

**场景**：有两台电脑（工作+家用），要同步代码和配置；或换了一台新电脑。

### 同步代码

| 你说什么 | AI 做什么 |
|---------|----------|
| "pull 一下" / "拉取最新" | `git pull` 拉取远端最新代码 |
| "提交" | commit + push（见[[#三、提交和同步代码|三、提交和同步代码]]） |

**多电脑工作原则**（详见 `规则/Git同步规则.md`）：
- 开工前先 `git pull`
- 结束前确保已 commit + push（否则另一台电脑拉不到）
- 写完文件不 commit 就关机 = 从 git 角度看等于没写

### 配定时任务（自动同步 + 自动总结）

| 你说什么 | AI 做什么 |
|---------|----------|
| "配定时任务" / "配自动化任务" | 读 `scripts/README.md`，引导你改占位符、装依赖、配计划任务/LaunchAgent |

**时间建议**：工作电脑 18:40 总结 / 18:50 同步，家用电脑 23:30 总结 / 23:50 同步。

### 换电脑 / 新电脑初始化

| 你说什么 | AI 做什么 |
|---------|----------|
| "换电脑了" / "新电脑初始化" | 读 `规则/新电脑初始化.md`，引导你装依赖、配软链、配定时任务、验证 |
| "检查一下环境" / "环境一致性检查" | 检查软链是否正确、git 是否最新、依赖是否装了 |

---

## 六、维护规则和文档

**场景**：想加一条规则、定个规矩、或代码改了要同步文档。

| 你说什么 | AI 做什么 |
|---------|----------|
| "加条规则" / "定个规矩" | AI 先问清规则内容，判断该写哪个文件，写完触发规则测试员检查 |
| "XX规则写哪" | AI 按规则文件职责边界表告诉你该写哪个文件 |
| "更新文档" / "同步文档" | 触发**文档维护员**，代码改了什么文档跟着改 |

**规则写哪个文件**（AI 会帮你判断，你也可以直接说）：
- 产品该有什么功能/字段 → `PRODUCT.md`
- 代码怎么写/文件怎么组织 → `AGENTS.md`
- 页面长什么样/用什么颜色 → `DESIGN.md`
- 某个具体场景的操作规范 → `规则/` 目录下对应文件
- AI 又犯了什么错 → `AI自检清单.md`

---

## 七、Skill

**场景**：想把一个重复流程固化成 skill，或查看现有 skill。

| 你说什么 | AI 做什么 |
|---------|----------|
| "做个skill" / "固化成skill" | AI 判断流程是否值得固化，按 `规则/Skill管理规则.md` 创建（含防重复检查） |
| "这个能不能做成skill" | 触发判断：流程是否重复、是否有固定步骤、是否值得维护 |
| "看看有哪些skill" | 列出当前全局 skill 目录里的 skill |

**Skill 存放规则**：统一放全局目录 `~/.config/opencode/skills/`，不区分项目级和全局级。详细规范见 `规则/Skill管理规则.md`。

---

## 八、查状态

**场景**：想知道项目做到哪了、改了哪些文件、最近做了什么。

| 你说什么 | AI 做什么 |
|---------|----------|
| "现在做到哪了" / "项目状态" | 读 AGENTS.md 的当前状态、git log、产出目录，告诉你进展 |
| "改了哪些文件" / "git status" | 列出当前未提交的改动 |
| "最近做了什么" / "看下日志" | 看 git log 最近的提交记录 |

---

## 九、Agent 协作

**场景**：想手动触发某个 Agent 帮你检查。大部分 Agent 会自动触发，你也可以手动叫。

| 你想做什么 | 你说什么 | 触发哪个 Agent |
|-----------|---------|--------------|
| 开发需求（走完整流程） | "做个功能" / "改一下" / "加个" / "走流程" | 项目总监（分发任务） |
| 评估技术方案/风险 | "评估技术方案" / "技术风险" / "技术总监" | 技术总监 |
| 拆解需求 | "拆解一下" / "先别急着改" | 需求拆解员 |
| 评估架构 | "评估架构" / "会不会影响结构" | 架构师 |
| 同步文档 | "更新文档" / "同步文档" | 文档维护员 |
| 检查代码遗漏 | "测一下" / "测试一下" | 代码测试员（增量） |
| 全量测试 | "全量测试" / "周测试" | 代码测试员（全量） |
| 检查规则/索引同步 | "查规则" / "规则检查" | 规则测试员 |
| 全流程验收 | "总监检查" / "自检" | 质量总监 |

**自动触发的 Agent**（不用你说）：
- 用户说"做个功能"等需求类表达 → 项目总监接管，调需求拆解员 → 技术总监接手技术部分 → 项目总监完成文档和验收
- 双总监制衡：项目总监管需求方向，技术总监管技术方案，僵持时升级给你

**Agent 协作流程**：
```
你说需求
  → 项目总监：理解需求，调需求拆解员
  → 需求拆解员：拆成清单
  → 技术总监：接手，调架构师评估，安排编码
  → 主Agent 编码
  → 技术总监：调代码测试员、规则测试员
  → 项目总监：调文档维护员
  → 项目总监：调质量总监验收
```

**流程兜底机制（防止卡死）**：

1. **轮数兜底**：审查类环节（技术总监评审、架构师评估、代码测试员检查）最多改 3 轮。第 3 轮还没通过，停止流程，把问题汇总给用户决定（继续改/换方案/放弃）。不无限来回踢皮球。

2. **状态协议**：审查类 agent 的报告末尾必须给出明确结论：
   - `STATUS: PASS` — 通过，进入下一步
   - `STATUS: NEEDS_REVISION` — 不通过，列出具体问题，打回上一步
   - 没有明确 STATUS 的报告 = 没审完，不能进入下一步

---

## 十、Agent

**场景**：想把一个需要判断和协作的角色固化成 Agent，或查看/管理现有 Agent。

| 你说什么 | AI 做什么 |
|---------|----------|
| "做个agent" / "加个agent" | AI 先问清这个 Agent 负责什么、在哪个阶段介入、做什么判断，然后在 `agents/` 下创建 .md 文件 |
| "这个流程能不能做成agent" | AI 判断：是需要判断+协作的角色 → 做 agent；是固定流程脚本 → 做 skill |
| "看看有哪些agent" | 列出 `agents/` 目录下的 Agent |
| "改一下XX agent" | 修改对应 agent 文件的描述/触发条件/检查项 |

**Agent 和 Skill 的区别**：
- **skill** 是"流程脚本"——你说一句话触发，按固定流程执行，产出固定结果（如周报、版本更新记录）
- **agent** 是"协作者"——在特定阶段介入，有判断能力，能跟主 Agent 协作（如需求拆解员、测试员）

简单判断：流程固定、不需要交互 → skill；需要判断、跟主 Agent 配合 → agent。

**Agent 存放规则**：放项目的 `agents/` 目录或 `.skills-mirror/agents/`（配了软链的话）。opencode 自动加载。详见 `规则/Skill管理规则.md` 的"skill 和 agent 的区别"。

---

## 速查：最常用的5句话

| 你想…… | 你说 | AI 做 |
|--------|------|------|
| 加功能 | "做个XX功能" | 项目总监分发 → 需求拆解 → 你确认 → 编码 |
| 提交代码 | "提交" | commit + push 一气呵成 |
| 改完检查 | "测一下" | 代码测试员检查遗漏 |
| 全流程检查 | "总监检查" | 质量总监全流程验收 |
| 收尾 | "好了" | 检查未提交改动，提交+推送 |

---

*提示词手册 · project-base · 2026-08-04*
