# AI 自检清单（持续追加）

> 这是 AI 在开发本项目时反复犯的问题清单。每次开工前扫一遍，完成功能后对照自检，避免重犯。
> 发现新问题就追加进来，格式见文末"如何追加"。

---

## 一、流程/协作类

### 1. 主动暴露盲区，不要等用户问

- **错误表现**：技术选型时用户问一个答一个，用户不问就不提。用户懂能问到，不懂的就成了盲区，遗漏到后期才发现。
- **根因**：AI 被动等用户问，而不是主动把"这个决策牵涉哪些点"铺给用户看。把"用户没问"等同于"不重要"。
- **检查动作**：回答技术问题时，主动列出"这个决策还牵涉哪些方面"，让用户看到全貌。该主动发现的坑（如某技术有隐藏前提/依赖），第一轮就要发现并告知。
- **首次发现**：2026-08-01（raisenote 项目），兼容性、数据备份、Vant依赖Vue 都是用户自己问到才暴露，AI 从没主动提

### 2. 讨论要在对的阶段，不要跨阶段提问

- **错误表现**：用户还在定技术选型，AI 就跳到需求细节去问"列表页显示什么标题"，用户被跨阶段的问题搞懵了。
- **根因**：AI 没有阶段意识，把所有待定问题一锅端，不分"现在该讨论的"和"后面才讨论的"。
- **检查动作**：先搞清楚当前在哪个阶段（技术选型？需求讨论？画原型？开发？），只讨论当前阶段的问题，后续阶段的问题先记下来。用户的工作流程是瀑布式：讨论需求 → 列需求清单 → 画原型 → 开发。
- **首次发现**：2026-08-01（raisenote 项目），用户还在定技术选型，AI 就问"列表页标题显示什么"

---

## 二、格式/正则类

（暂无，发现后追加）

---

## 三、路径/环境类

### 1. 配了模型 capabilities 不验证 API 端是否真支持

- **错误表现**：在 `opencode.jsonc` 里给模型加了 `"capabilities": ["image"]`，告诉 opencode 客户端"这个模型支持图片输入"。用户发图后模型回复"我是文本 AI，无法处理图片"。AI 没有第一时间验证 API 端是否真的转发图片字段，而是先怀疑配置没生效、重启没重启。
- **根因**：`capabilities` 只是客户端侧的声明，告诉 opencode"允许给这个模型发图片"。但图片能不能到模型手里，取决于 API 网关是否转发 `image_url` 字段。配了 capabilities ≠ API 真能用，两者是声明和实现的关系。跳过了端到端验证，在客户端配置上空转。
- **检查动作**：配了模型能力（image/vision/function_calling 等）后，必须端到端验证：用 curl 向 API 发一个最小请求（如 1x1 图片 + "什么颜色"），看模型返回的是图片内容还是"我不支持"。客户端配置只是第一步，API 端真正支持才算数。第三方 API 网关可能不转发多模态字段，不能假设"官方模型支持=这个 API 也支持"。
- **首次发现**：2026-08-05（workbench 项目），glm-5.2 通过秒云 API 调用，配了 capabilities:["image"] 后用户发图，模型回复"text-based AI cannot see image"。curl 验证发现 prompt_tokens 只有 35（图片字段被网关丢弃），确认秒云 API 不支持多模态。

---

## 四、前端/渲染类

（暂无，发现后追加）

---

## 五、后端/数据类

（暂无，发现后追加）

---

## 如何追加

发现新的反复犯的问题时，按以下格式追加到对应分类下：

```
### N. 一句话标题

- **错误表现**：具体怎么错的，现象是什么
- **根因**：为什么会错（深层原因，不是表面现象）
- **检查动作**：以后怎么避免，具体检查什么（可执行的步骤）
- **首次发现**：YYYY-MM-DD，简述当时的情况
```

分类满了就新建一个 `## 六、xxx类`。不确定归哪类就放"流程/协作类"或新建分类。

---

## 文档变更记录

| 日期 | 变更 |
|------|------|
| 【填写日期】 | 初版，基于 project-base 地基包创建 |
| 2026-08-05 | 新增路径/环境类第1条：配了模型 capabilities 不验证 API 端是否真支持（来源：workbench 项目秒云 API 识图失败） |
