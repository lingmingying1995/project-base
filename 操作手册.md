# 操作手册 · 从地基包启动新项目

> 本文档是 AI 读的。当用户 clone 了地基包、打开 opencode 后，AI 先读本文件，引导用户完成项目初始化。
> 用户也可以读这个文档了解完整流程。

---

## AI 读取本文件后的行为

1. 先读完本文件，了解完整流程
2. 用 3 句话告诉用户：地基包是什么、接下来要做什么、大概需要几分钟
3. 按下方"初始化流程"逐步引导用户，**每一步等用户确认后再进入下一步**
4. 全部完成后，告诉用户"地基已搭好，可以开始干活了"

---

## 初始化流程（共6步）

### 第1步：确认项目基本信息

问用户三个问题，等用户回答：

1. **项目名**：叫什么（英文或中文都行）
2. **项目是做什么的**：一两句话说清
3. **技术栈**：前端/后端/数据库用了什么
   - 用户不懂技术就给 2-3 个方案让用户选，不替用户定
   - 不要自己加约束（如"零学习成本"），约束来自用户需求

**确认**：把三个信息复述一遍，等用户说"对"再进入下一步。

---

### 第2步：填写项目文件

根据用户回答，修改以下文件。**只改【】里的提示语，不要动其他内容**。

#### 必改的文件（4个）

| 文件 | 改什么 | 怎么改 |
|------|--------|--------|
| `AGENTS.md` | 项目名、项目描述、技术栈、日期 | 把 `【项目名】`、`【填写项目描述...】`、`【填写技术栈...】`、`【填写日期】` 替换成实际内容 |
| `PRODUCT.md` | 产品定位、目标用户、功能范围 | 把 `【填写...】` 提示语替换成实际内容。其他空骨架部分（一句话定位、核心价值等）引导用户填，填不了的先留空 |
| `DESIGN.md` | 配色、字号、组件样式 | **如有UI才填**。纯后端项目跳过。引导用户填主色、布局，填不了的先留空 |
| `AI自检清单.md` | 日期 | 把 `【填写日期】` 替换成实际日期 |

#### 不用改的文件（成品，直接用）

| 文件 | 为什么不用改 |
|------|-------------|
| `AGENTS.md` 的"确认规则"2条 | 这是 AI 协作的核心规则，通用，不用改 |
| `AGENTS.md` 的开发规则、高风险操作、规则文件职责边界 | 通用，不用改 |
| `提示词手册.md` | 给用户的提示词速查，通用，不用改 |
| `规则/文件组织规则.md` | 通用规则，不用改 |
| `规则/项目索引规则.md` | 通用规则，不用改（按需追加项目） |
| `规则/Git同步规则.md` | 通用规则，不用改 |
| `规则/自动化任务规则.md` | 通用规则，按需配任务 |
| `规则/新电脑初始化.md` | 骨架，按需填项目特有的路径和脚本 |
| `规则/Skill管理规则.md` | 通用规则，不用改（如不用 opencode 则跳过） |
| `scripts/*.bat` / `*.sh` / `*.js` / `*.plist` | 自动化任务脚本模板，第5步会引导替换占位符（详见 scripts/README.md） |
| `agents/*.md`（10个Agent） | 通用，不用改（含 project-director 项目总监、tech-director 技术总监、task-guardian 定时任务守护） |

#### 按需创建的目录

| 目录 | 什么时候创建 | 用来放什么 |
|------|------------|-----------|
| `产出/每日总结/` | 开始开发后 | 每天的对话总结（AI自动写） |
| `产出/决策记录/` | 产生有价值决策时 | 决策过程沉淀（AI主动记） |
| `产出/任务日志/` | 配置定时任务后 | 定时任务执行日志（脚本自动写） |
| `产出/测试报告/` | 配置全量测试后 | 全量测试报告（脚本自动写） |

---

### 第3步：配置 Git

```bash
# 1. 改 git remote 指向新仓库（先在 GitHub 上建好空仓库）
git remote set-url origin <新仓库地址>

# 2. 提交初始化
git add -A
git commit -m "feat: 基于地基包初始化项目"
git push
```

如果用户还没建 GitHub 仓库，提醒用户先建，建完再继续。

---

### 第4步：配置 Agent 和 Skill（如使用 opencode）

#### 4.1 配置 Agent

地基包里有 10 个开发协作 Agent（含 project-director 项目总监、tech-director 技术总监、task-guardian 定时任务守护），需要放到 opencode 能加载的位置：

**方式A：项目级（推荐，跟项目走）**
```bash
# 在项目根目录创建 .skills-mirror/agents/ 目录
mkdir -p .skills-mirror/agents
cp agents/*.md .skills-mirror/agents/
```

**方式B：全局级（所有项目共享）**
```bash
# 复制到 opencode 全局 agents 目录
cp agents/*.md ~/.config/opencode/agents/
```

如果用户配了工作台的软链（`~/.config/opencode/agents/` 指向工作台的 `.skills-mirror/agents/`），直接复制到工作台的 `.skills-mirror/agents/` 即可。

**Task Guardian Agent 说明**：`task-guardian.md` 是定时任务守护 Agent，配合 `scripts/task-guardian-*.bat` 和 `schtasks-xml/` 使用。它会在定时任务执行时自动诊断和修复常见问题（目录缺失、模型不可用等）。

#### 4.2 配置 Skill 目录

所有 skill 统一放全局目录 `~/.config/opencode/skills/`，不建项目级目录。skill 的创建规则、写作规范见 `规则/Skill管理规则.md`。

#### 4.3 全局 Skill 软链配置（可选，主项目才配）

如果本项目是"主项目"（愿意承载全局 skill），按软链方式配置：

**先检查全局目录是否已经是软链：**
```bash
ls -la ~/.config/opencode/skills
```

- **已是软链**（指向某个项目的镜像目录）→ 说明已有主项目，本项目不用配。本项目想创建 skill 直接写到全局目录（通过软链访问主项目镜像）
- **不是软链**（普通目录或不存在）→ 本项目可以当主项目，执行：
  ```bash
  # 建镜像目录
  mkdir -p .skills-mirror/skills
  mkdir -p .skills-mirror/agents
  # 删掉全局目录（先备份里面的 skill！）
  mv ~/.config/opencode/skills ~/.config/opencode/skills.bak
  # 创建软链
  ln -s $(pwd)/.skills-mirror/skills ~/.config/opencode/skills
  ln -s $(pwd)/.skills-mirror/agents ~/.config/opencode/agents
  ```
  配好后，全局 skill 和 agent 跟着本项目 git 走，换电脑 git pull 即同步。

**判断该不该当主项目：**
- 本项目是你的核心工作项目、长期使用 → 可以当主项目
- 本项目是一次性项目或临时项目 → 不要当主项目，skill 直接写全局目录（走现有主项目的软链）
- 已经有其他项目当主项目了 → 不要重复配，全局只能指向一个地方

详细机制见 `规则/Skill管理规则.md` 第五节"跨电脑同步"。

**不使用 opencode 的用户跳过这步**，Agent 和 Skill 文件不影响项目本身。

---

### 第5步：配置自动化任务（定时同步 + 每日总结）

> 这一步容易漏。只复制脚本文件不注册计划任务 = 定时任务不会跑，每天得手动做总结和同步。
> 如果用户明确说"暂不需要定时任务"，跳过本步，但要在第6步验证清单里勾"确认暂不需要"。

按以下顺序操作，**每步等用户确认后再进入下一步**：

#### 5.1 替换脚本占位符

搜索 `scripts/` 下所有文件里的 `PROJECT_NAME` 和 `PROJECT_PATH`，替换成项目英文名和项目绝对路径。具体改哪些见 `scripts/README.md` 的"使用前必改"表。

**必改的文件**（不全改完定时任务跑不起来）：
- `sync.bat` / `sync.sh` — PROJECT_NAME（日志文件名用）
- `write-task-log.ps1` — `$workMachines` 数组（工作电脑机器名，`echo %COMPUTERNAME%` 查看）
- `auto-summary.bat` — 日志文件名里的 PROJECT_NAME
- `auto-summary.js` — `CONFIG.projectKeyword`、`CONFIG.logFile`、prompt 里的项目名
- 两个 plist（如用 Mac）— Label、路径、WorkingDirectory
- `schtasks-xml/*.xml`（如用 Task Guardian 模式）— PROJECT_NAME、PROJECT_PATH

#### 5.2 装依赖

`auto-summary.js` 需要 `sql.js` 读 opencode.db：
```bash
cd server && npm install sql.js
```
项目没有 server 目录就建一个再装，或改 `auto-summary.js` 里的 `serverDir` 指向有 sql.js 的目录。

#### 5.3 注册计划任务

先问用户：**这台电脑是工作电脑还是家用电脑？**（决定任务时间，避免两台电脑同时跑产生 git 冲突）

| 电脑角色 | 每日总结 | Git 同步 | 任务名 |
|---------|---------|---------|--------|
| 工作电脑 | 18:40 | 18:50 | `PROJECT_NAME_AutoSummary` / `PROJECT_NAME_Sync` |
| 家用电脑 | 23:30 | 23:50 | 同上（文件名加 `-HOME` 后缀区分） |

**方式A：传统脚本模式**（简单，无额外依赖）
```cmd
schtasks /create /tn "PROJECT_NAME_Sync" /tr "PROJECT_PATH\scripts\sync.bat" /sc daily /st 18:50 /f
schtasks /create /tn "PROJECT_NAME_AutoSummary" /tr "PROJECT_PATH\scripts\auto-summary.bat" /sc daily /st 18:40 /f
```

**方式B：Task Guardian 模式**（失败时自动诊断修复，前置：装 opencode CLI + 配 API Key + 放 task-guardian.md。`task-guardian-summary.bat`/`task-guardian-sync.bat` 是另一种调用方式，详见 scripts/README.md）
```cmd
schtasks /create /xml "PROJECT_PATH\scripts\schtasks-xml\PROJECT_NAME_AutoSummary.xml" /tn "\PROJECT_NAME_AutoSummary" /f
schtasks /create /xml "PROJECT_PATH\scripts\schtasks-xml\PROJECT_NAME_Sync.xml" /tn "\PROJECT_NAME_Sync" /f
```

**Mac 用 launchd**：改完 plist 里的 WorkingDirectory 后
```bash
cp scripts/com.PROJECT_NAME.*.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.PROJECT_NAME.auto-summary.plist
launchctl load ~/Library/LaunchAgents/com.PROJECT_NAME.sync.plist
```

#### 5.4 验证定时任务能跑

手动触发一次，确认不报错：
```cmd
schtasks /run /tn "PROJECT_NAME_AutoSummary"
schtasks /query /tn "PROJECT_NAME_AutoSummary" /v /fo LIST
```

跑完后检查 `产出/每日总结/` 有没有生成今天的总结文件，`产出/任务日志/` 有没有日志。有问题按报错排查（常见：路径含空格没加引号、sql.js 没装、占位符没替干净）。

---

### 第6步：验证 + 开始干活

检查清单：
- [ ] AGENTS.md 里的【】提示语全部替换完了
- [ ] PRODUCT.md 填了产品定位和功能范围
- [ ] DESIGN.md 填了（如有UI）或跳过了（纯后端）
- [ ] AI自检清单.md 的日期填了
- [ ] git remote 改了，push 成功了
- [ ] Agent 配置好了（如用 opencode）
- [ ] 全局软链配好了或确认不需要（如用 opencode 且本项目当主项目）
- [ ] scripts/ 里的 PROJECT_NAME 和 PROJECT_PATH 占位符替换完了
- [ ] auto-summary.js 依赖装了（cd server && npm install sql.js）
- [ ] 定时任务注册并验证通过（或确认暂不需要）

全部完成后告诉用户：

> 地基已搭好。包含：
> - 开发规则（AGENTS.md）—— AI 每次开工自动读
> - 产品说明（PRODUCT.md）—— 定义做什么、不做什么
> - 设计规范（DESIGN.md）—— 画UI时参考
> - AI自检清单 —— 边做边加，防重犯
> - 10个协作 Agent —— 项目总监→技术总监→需求拆解→架构→编码→UI设计→文档→测试→验收→定时任务守护（双总监制衡）
> - 6个规则文件 —— 文件组织、项目索引、Git同步、自动化任务、新电脑初始化、Skill管理
> - 自动化脚本模板 —— 改占位符后配定时任务，实现自动同步+每日总结
> - 提示词手册（提示词手册.md）—— 不知道对AI说什么时翻这个
>
> 现在可以开始了。告诉我第一件事要做什么。

---

## 各文件详细说明

### AGENTS.md（开发规则）

**AI 每次开工自动读的文件，最重要的一个。**

包含两部分：
1. **项目信息**（要填）：项目名、描述、技术栈、日期——在文件开头
2. **协作规则**（不用动）：
   - "确认规则"2条——AI协作的核心规则
   - 开工前必读清单
   - 目录结构（改完项目信息后可以更新）
   - 开发规则（路径、代码、Git、命令输出）
   - 高风险操作
   - AI自检清单引用
   - 规则文件职责边界

### PRODUCT.md（产品说明）

**定义"做什么、不做什么"的文件。**

要填的：
- 产品定位（一句话说清是什么、给谁用、解决什么问题）
- 目标用户（用户画像、核心需求）
- 功能范围（做什么、不做什么、成功标准）

可以留空的：版本规划（等做的时候再填）

### DESIGN.md（设计规范）

**画UI时参考的文件。纯后端项目跳过。**

要填的：
- 整体布局
- 配色方案（至少填主色）
- 字号
- 组件样式（按钮、卡片等，边做边填）
- 交互原则

可以留空的：具体组件细节（边做边补）

### AI自检清单.md

**AI 边做边加的犯错清单。**

初始是空骨架（5个分类，都是"暂无"）。开发过程中 AI 犯了错就追加，格式见文件底部的"如何追加"。

### 规则/文件组织规则.md

**文件该放哪、怎么组织的规则。不用改。**

包含：
- 文件分工（什么内容写哪个文件）
- 一个内容单元一个独立文件（不叠加）
- 认知/灵感类文档必须带原话
- 对话自动记录规则（记什么、怎么记、什么时候记）
- 索引维护规则
- 新增文件检查动作

### 规则/Git同步规则.md

**什么时候提交、怎么提交的规则。不用改。**

核心原则：攒着改，触发时统一提交（用户说push、对话结束、切换电脑时提交）。

### 规则/新电脑初始化.md

**换电脑时配环境的骨架。按需填项目特有的路径和脚本。**

### 规则/项目索引规则.md

**跨项目访问时的项目名查询表。不用改骨架，按需追加项目。**

AI 需要访问同级其他项目时，从此表查项目目录名，用相对路径 `../项目名` 访问，不硬编码绝对路径。

### agents/（10个协作Agent）

**双总监制衡，覆盖"需求→分发→编码→验收→定时任务守护"全流程。不用改。**

| Agent | 何时用 | 做什么 |
|-------|--------|--------|
| project-director | 用户提需求时 | 管需求方向、流程调度、业务取舍 |
| tech-director | 项目总监交接后 | 管技术方案、架构风险、代码质量 |
| requirement-analyst | 被项目总监调用 | 拆成改动清单 |
| architect | 被技术总监调用 | 评估结构影响 |
| ui-designer | 编码后涉及UI时/被项目总监调用 | 改前端样式和交互 |
| doc-maintainer | 被项目总监调用 | 同步文档 |
| code-tester | 被技术总监调用 | 检查代码遗漏 |
| rule-tester | 被技术总监调用 | 检查索引/规则/手册同步 |
| quality-director | 全流程后 | 全流程验收，检查完整性 |
| task-guardian | 定时任务执行时 | 执行每日总结/Git同步，失败自动诊断修复 |

### 规则/Skill管理规则.md

**创建 opencode skill 时遵循的规范。不用改。**

包含：
- 统一放全局目录的存放规则（不区分项目级和全局级）
- 创建前的防重复检查（扫全局目录 + 触发关键词撞车消歧）
- SKILL.md 写作规范（frontmatter、撞车消歧、正文结构、语言要求）
- 跨电脑同步方案（独立git仓库 / 软链绑定主项目）
- skill 和 agent 的区别（流程固定→skill，需要判断协作→agent）

不使用 opencode 的项目跳过本文件。

### scripts/（自动化任务脚本模板）

**定时同步和每日总结的可执行脚本。改占位符后用。**

包含：
- `sync.bat` / `sync.sh` — Git 自动同步（Windows / Mac）
- `auto-summary.bat` / `auto-summary.js` — 每日对话归档
- `com.PROJECT_NAME.*.plist` — Mac 定时任务配置
- `task-guardian-summary.bat` / `task-guardian-sync.bat` — Task Guardian 模式包装器（Windows）
- `schtasks-xml/` — Windows 计划任务 XML 模板（支持路径含空格）
- `README.md` — 使用说明（改哪些占位符、怎么配定时任务）

使用前搜索所有文件里的 `PROJECT_NAME` 替换成项目英文名、`PROJECT_PATH` 替换成项目绝对路径，详见 `scripts/README.md`。

---

## 维护手册：新增/修改规则文件时的同步清单

> 地基包的规则文件被多处引用，新增或修改一个规则文件时，必须同步更新以下所有关联点。
> 漏一个就会出现"文件存在但索引没提""数量对不上"等不一致问题。
> **每次新增/修改规则文件后，对照本清单逐项检查。**

### 新增规则文件（在 `规则/` 下建新 .md）要同步改的地方

| # | 改哪个文件 | 改什么 | 怎么改 |
|---|----------|--------|--------|
| 1 | `规则/文件组织规则.md` | "当前规则目录内容"表（第八节） | 加一行：文件名 + 内容 + 被谁引用 |
| 2 | `操作手册.md` | "不用改的文件"表（第2步） | 加一行：文件名 + 为什么不用改 |
| 3 | `操作手册.md` | "各文件详细说明"章节 | 加一个小节：文件名 + 包含什么 + 通用还是按需填 |
| 4 | `操作手册.md` | 完成提示语（第6步末尾） | "X个规则文件"的数字 +1，并更新列表 |
| 5 | `AGENTS.md` | 目录结构树（第四节） | 在 `规则/` 下加一行 |
| 6 | `AGENTS.md` | 文档变更记录（第九节） | 加一行：日期 + 新增了什么 |
| 7 | `README.md` | 目录树 | 在 `规则/` 下加一行 |
| 8 | `提示词手册.md` | 如新规则有用户可触发的操作 | 在对应场景表加一行：你说什么 → AI 做什么 |
| 9 | `提示词手册.md` | 如新规则对应 Agent 触发 | Agent 协作表加一行（如适用） |

### 修改现有规则文件（改内容，不增删文件）要检查的地方

| # | 查哪个文件 | 查什么 |
|---|----------|--------|
| 1 | `AGENTS.md` 规则文件职责边界表 | 新内容是否落在这条规则的职责范围内，没越界 |
| 2 | `操作手册.md` 对应的详细说明小节 | 描述是否还准确（如"通用不用改"是否还成立） |
| 3 | `提示词手册.md` | 如改了触发词或流程，提示词表是否要同步更新 |
| 4 | `AGENTS.md` 文档变更记录 | 加一行：日期 + 改了什么 |

### 删除规则文件（极少见）要同步改的地方

删除 = 新增的反向操作。除了上面新增清单的1-8项反向操作（删行、数字-1），还要：
- 确认没有其他文件还在引用被删的文件（用全文搜索文件名）
- 确认 `提示词手册.md` 里没有引用被删文件的触发词

### 通用原则

- **改完必须触发规则测试员**：说"查规则"或"规则检查"，让 rule-tester 跑一遍一致性检查
- **数字一致性是重灾区**：凡是有"X个规则文件""X个Agent"的地方，加减文件时全部要同步改
- **不确定要改哪些时**：全文搜索被改文件的文件名，所有提到它的地方都是候选改动点
- **不只是规则文件**：在根目录新增 .md 文件（如提示词手册.md）、agents/ 下新增 agent、scripts/ 下新增脚本时，同样按本清单的思路同步目录树（AGENTS.md / README.md）、数量描述、操作手册"不用改的文件"表

---

## 常见问题

### Q：地基包里的规则会不会约束AI太死？

不会。地基里的规则管的是"方向和底线"（确认规则、不替用户决定），不管"创意和判断"。AI在创意、判断、角度上要主动发挥。

### Q：可以改地基包里的规则文件吗？

可以。clone 下来后就是你的项目，想怎么改怎么改。但建议先按原样用，跑顺了再改。

### Q：地基包更新了，已建的项目怎么办？

已建的项目不会自动更新（各自独立演进）。如果地基包加了新规则，手动复制过来合并。

### Q：不用 opencode 能用这个地基包吗？

能。AGENTS.md、PRODUCT.md、规则文件都是纯 markdown，任何 AI 工具都能读。Agent 文件是 opencode 专属的，不用 opencode 就跳过 agents/ 目录。

---

*操作手册 · project-base · 2026-08-02*
