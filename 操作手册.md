# 操作手册 · 从地基包启动新项目

> 本文档是 AI 读的。当用户 clone 了地基包、打开 opencode 后，AI 先读本文件，引导用户完成项目初始化。
> 用户也可以读这个文档了解完整流程。

---

## AI 读取本文件后的行为

1. 先读完本文件，了解完整流程
2. 用 3 句话告诉用户：地基包是什么、接下来要做什么、大概需要几分钟
3. 按下方"初始化流程"逐步引导用户，**每一步等用户确认后再进入下一步**
4. 全部完成后，告诉用户"地基已搭好，可以开始干活了"

---

## 初始化流程（共5步）

### 第1步：确认项目基本信息

问用户三个问题，等用户回答：

1. **项目名**：叫什么（英文或中文都行）
2. **项目是做什么的**：一两句话说清
3. **技术栈**：前端/后端/数据库用了什么
   - 用户不懂技术就给 2-3 个方案让用户选，不替用户定
   - 不要自己加约束（如"零学习成本"），约束来自用户需求

**确认**：把三个信息复述一遍，等用户说"对"再进入下一步。

---

### 第2步：填写项目文件

根据用户回答，修改以下文件。**只改【】里的提示语，不要动其他内容**。

#### 必改的文件（4个）

| 文件 | 改什么 | 怎么改 |
|------|--------|--------|
| `AGENTS.md` | 项目名、项目描述、技术栈、日期 | 把 `【项目名】`、`【填写项目描述...】`、`【填写技术栈...】`、`【填写日期】` 替换成实际内容 |
| `PRODUCT.md` | 产品定位、目标用户、功能范围 | 把 `【填写...】` 提示语替换成实际内容。其他空骨架部分（一句话定位、核心价值等）引导用户填，填不了的先留空 |
| `DESIGN.md` | 配色、字号、组件样式 | **如有UI才填**。纯后端项目跳过。引导用户填主色、布局，填不了的先留空 |
| `AI自检清单.md` | 日期 | 把 `【填写日期】` 替换成实际日期 |

#### 不用改的文件（成品，直接用）

| 文件 | 为什么不用改 |
|------|-------------|
| `AGENTS.md` 的"先确认再动手"7条 | 这是 AI 协作的核心规则，通用，不用改 |
| `AGENTS.md` 的开发规则、高风险操作、规则文件职责边界 | 通用，不用改 |
| `规则/文件组织规则.md` | 通用规则，不用改 |
| `规则/Git同步规则.md` | 通用规则，不用改 |
| `规则/新电脑初始化.md` | 骨架，按需填项目特有的路径和脚本 |
| `agents/*.md`（4个Agent） | 通用，不用改 |

#### 按需创建的目录

| 目录 | 什么时候创建 | 用来放什么 |
|------|------------|-----------|
| `每日总结/` | 开始开发后 | 每天的对话总结（AI自动写） |
| `决策记录/` | 产生有价值决策时 | 决策过程沉淀（AI主动记） |

---

### 第3步：配置 Git

```bash
# 1. 改 git remote 指向新仓库（先在 GitHub 上建好空仓库）
git remote set-url origin <新仓库地址>

# 2. 提交初始化
git add -A
git commit -m "feat: 基于地基包初始化项目"
git push
```

如果用户还没建 GitHub 仓库，提醒用户先建，建完再继续。

---

### 第4步：配置 Agent（如使用 opencode）

地基包里有 5 个开发协作 Agent，需要放到 opencode 能加载的位置：

**方式A：项目级（推荐，跟项目走）**
```bash
# 在项目根目录创建 .skills-mirror/agents/ 目录
mkdir -p .skills-mirror/agents
cp agents/*.md .skills-mirror/agents/
```

**方式B：全局级（所有项目共享）**
```bash
# 复制到 opencode 全局 agents 目录
cp agents/*.md ~/.config/opencode/agents/
```

如果用户配了工作台的软链（`~/.config/opencode/agents/` 指向工作台的 `.skills-mirror/agents/`），直接复制到工作台的 `.skills-mirror/agents/` 即可。

**不使用 opencode 的用户跳过这步**，Agent 文件不影响项目本身。

---

### 第5步：验证 + 开始干活

检查清单：
- [ ] AGENTS.md 里的【】提示语全部替换完了
- [ ] PRODUCT.md 填了产品定位和功能范围
- [ ] DESIGN.md 填了（如有UI）或跳过了（纯后端）
- [ ] AI自检清单.md 的日期填了
- [ ] git remote 改了，push 成功了
- [ ] Agent 配置好了（如用 opencode）

全部完成后告诉用户：

> 地基已搭好。包含：
> - 开发规则（AGENTS.md）—— AI 每次开工自动读
> - 产品说明（PRODUCT.md）—— 定义做什么、不做什么
> - 设计规范（DESIGN.md）—— 画UI时参考
> - AI自检清单 —— 边做边加，防重犯
> - 5个协作 Agent —— 需求拆解→架构→编码→文档→测试→验收
> - 3个规则文件 —— 文件组织、Git同步、新电脑初始化
>
> 现在可以开始了。告诉我第一件事要做什么。

---

## 各文件详细说明

### AGENTS.md（开发规则）

**AI 每次开工自动读的文件，最重要的一个。**

包含两部分：
1. **项目信息**（要填）：项目名、描述、技术栈、日期——在文件开头
2. **协作规则**（不用动）：
   - "先确认再动手"7条——AI协作的核心规则
   - 开工前必读清单
   - 目录结构（改完项目信息后可以更新）
   - 开发规则（路径、代码、Git、命令输出）
   - 高风险操作
   - AI自检清单引用
   - 规则文件职责边界

### PRODUCT.md（产品说明）

**定义"做什么、不做什么"的文件。**

要填的：
- 产品定位（一句话说清是什么、给谁用、解决什么问题）
- 目标用户（用户画像、核心需求）
- 功能范围（做什么、不做什么、成功标准）

可以留空的：版本规划（等做的时候再填）

### DESIGN.md（设计规范）

**画UI时参考的文件。纯后端项目跳过。**

要填的：
- 整体布局
- 配色方案（至少填主色）
- 字号
- 组件样式（按钮、卡片等，边做边填）
- 交互原则

可以留空的：具体组件细节（边做边补）

### AI自检清单.md

**AI 边做边加的犯错清单。**

初始是空骨架（5个分类，都是"暂无"）。开发过程中 AI 犯了错就追加，格式见文件底部的"如何追加"。

### 规则/文件组织规则.md

**文件该放哪、怎么组织的规则。不用改。**

包含：
- 文件分工（什么内容写哪个文件）
- 一个内容单元一个独立文件（不叠加）
- 认知/灵感类文档必须带原话
- 对话自动记录规则（记什么、怎么记、什么时候记）
- 索引维护规则
- 新增文件检查动作

### 规则/Git同步规则.md

**什么时候提交、怎么提交的规则。不用改。**

核心原则：攒着改，触发时统一提交（用户说push、对话结束、切换电脑时提交）。

### 规则/新电脑初始化.md

**换电脑时配环境的骨架。按需填项目特有的路径和脚本。**

### agents/（5个协作Agent）

**覆盖"需求→编码→验收"全流程。不用改。**

| Agent | 何时用 | 做什么 |
|-------|--------|--------|
| requirement-analyst | 用户提需求后、编码前 | 拆成改动清单 |
| architect | 需求拆解后、编码前 | 评估结构影响 |
| doc-maintainer | 编码后 | 同步文档 |
| code-tester | 编码后 | 检查遗漏 |
| quality-director | 全流程后 | 全流程验收，检查完整性 |

---

## 常见问题

### Q：地基包里的规则会不会约束AI太死？

不会。地基里的规则管的是"方向和底线"（先确认再动手、不替用户决定、主动暴露盲区），不管"创意和判断"。AI在创意、判断、角度上要主动发挥。

### Q：可以改地基包里的规则文件吗？

可以。clone 下来后就是你的项目，想怎么改怎么改。但建议先按原样用，跑顺了再改。

### Q：地基包更新了，已建的项目怎么办？

已建的项目不会自动更新（各自独立演进）。如果地基包加了新规则，手动复制过来合并。

### Q：不用 opencode 能用这个地基包吗？

能。AGENTS.md、PRODUCT.md、规则文件都是纯 markdown，任何 AI 工具都能读。Agent 文件是 opencode 专属的，不用 opencode 就跳过 agents/ 目录。

---

*操作手册 · project-base · 2026-08-02*
