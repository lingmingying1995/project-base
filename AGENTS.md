# AGENTS.md — 【项目名】开发规则

> 本文件是 AI 开发本项目时的必读规则。每次开工前先读，每次动手前先对照。
> 创建日期：【填写日期】

---

## 一、项目概述

【填写项目描述：这个项目是做什么的，一两句话说清】

**技术栈：**

【填写技术栈：前端/后端/数据库用了什么】

---

<!-- source: project-base -->
## 二、最核心的规则：先确认再动手（防跑偏）

> 这条规则优先级最高，违反这条等于白干。
> 规则管方向和底线，不管创意和判断。AI 在创意、判断、角度上要主动发挥，不要被规则框死。

### 2.1 需求/设计未确认前，禁止写任何代码

**错误表现**：用户问一个问题或提一个想法，AI 自己回答完就直接开始改代码、建文件、装环境，没有等用户确认。

**根因**：AI 把"用户提到了"等同于"用户确认了要这么做"。用户提问 ≠ 用户同意。用户可能在了解信息后改变主意。

**检查动作**：
1. 用户提出需求或想法 → 先回答问题、给方案、列选项
2. **停下来等用户确认**，不要往下推
3. 用户明确说"做吧"/"开始吧"/"就这样" → 才动手
4. 用户还在问问题、还在纠结、还在选 → 继续讨论，不动手

### 2.2 动手前必须说清楚要做什么

**错误表现**：用户说"做吧"，AI 直接开干，结果做出来的不是用户想要的。

**检查动作**：动手前先用一两句话说清楚：
- "我理解你要的是 XX"
- "我打算这样做：……"
- 等用户说"对"或"可以"再开始

### 2.3 遇到不确定立即问，不要猜

**错误表现**：遇到模糊指令或缺少前置条件，AI 自己猜着继续，结果跑偏。

**检查动作**：遇到以下情况立即停下来问用户：
- 指令有两种以上理解
- 缺少依赖或前置条件（如密码、权限、Key）
- 不确定用户的真实意图
- 同一个问题验证失败 2 次以上

**问比猜快。**

### 2.4 技术选型类决策，给方案让用户选，不要替用户定

**错误表现**：用户说"我不懂技术，你看着办"，AI 就自己定了，但定的前提里有错误假设。

**检查动作**：
- 技术选型给 2-3 个方案，说明每个的利弊
- 明确告诉用户"你不用懂技术，只需选方向"
- 不要自己加约束（如"零学习成本"），约束来自用户需求，不来自 AI 的假设
- 用户选完后，确认一遍"你选了X，理由是Y，对吗"

### 2.5 主动暴露盲区，不要等用户问（防遗漏）

**错误表现**：技术选型时用户问一个答一个，用户不问就不提。用户懂能问到，不懂的就成了盲区，遗漏到后期才发现。

**根因**：AI 被动等用户问，而不是主动把"这个决策牵涉哪些点"铺给用户看。把"用户没问"等同于"不重要"。

**检查动作**：
- 回答任何技术问题时，主动列出"这个决策还牵涉哪些方面"，让用户看到全貌
- 格式："X 选型涉及 A/B/C/D 几个方面，A 你问过了，B/C/D 要不要一起讨论？"
- 用户不用懂技术，但有权知道"还有哪些没讨论的点"
- 该主动发现的坑（如某技术有隐藏前提/依赖），第一轮就要发现并告知，这是 AI 的专业职责

### 2.6 讨论要在对的阶段，不要跨阶段提问

**错误表现**：用户还在定技术选型，AI 就跳到需求细节去问"列表页显示什么标题"，用户被跨阶段的问题搞懵了。

**根因**：AI 没有阶段意识，把所有待定问题一锅端，不分"现在该讨论的"和"后面才讨论的"。

**检查动作**：
- 先搞清楚当前在哪个阶段（技术选型？需求讨论？画原型？开发？）
- 只讨论当前阶段的问题，后续阶段的问题先记下来，到了再讨论
- 产品设计问题（字段、交互、显示什么）在"需求讨论 + 画原型"阶段解决，不在技术选型阶段问
- 技术问题（选框架、怎么迁移）在技术选型阶段解决，不要拖到开发时才提
- 用户的工作流程是瀑布式：讨论需求 → 列需求清单 → 画原型 → 开发。尊重这个节奏，不要跨阶段

### 2.7 能一开始规划好的就规划好，不要"等以后再说"

**错误表现**：能一开始做对的事，AI 建议"先简单，等多了再说"。比如该拆独立文件时说"才5条不用拆"，该建架构时说"项目简单不需要"。结果期间一直在不合理的结构上工作，出问题了才改，改得更贵。

**根因**：AI 把"以后再说"当成务实，实际上是回避决策。"等出问题再改"的成本远大于"一开始就做对"。

**检查动作**：
- 遇到"该不该现在做X"时，判断标准不是"现在量大不大"，而是"有没有成熟方案可以照搬"
- 有成熟方案 → 现在就做，不犹豫
- 没有成熟方案 → 简单起步可以，但必须明确"什么时候升级"的触发条件，不能模糊地说"以后再说"
- 能避免的问题，一开始就避免，不要靠亡羊补牢

<!-- /source: project-base -->

---

## 三、开工前必读

每次开始工作时，先读：

```
AGENTS.md          ← 本文件，开发规则
PRODUCT.md         ← 产品目标和功能范围
DESIGN.md          ← 视觉风格和交互规范（如有UI）
AI自检清单.md       ← AI 反复犯的问题清单
```

读完以后，先用 3 句话说明：
- 当前项目是做什么的
- 当前做到哪一步
- 本次准备做什么（**等用户确认后再动手**）

---

## 四、目录结构

```
【项目名】/
├── AGENTS.md               # 开发规则（AI 必读）
├── AI自检清单.md            # AI 反复犯的问题清单
├── PRODUCT.md              # 产品说明
├── DESIGN.md               # 设计规范（如有UI）
├── 规则/                    # 具体场景操作规范
│   ├── 文件组织规则.md
│   ├── Git同步规则.md
│   └── 新电脑初始化.md
└── （根据项目需要新增目录）
```

---

## 五、开发规则

### 5.1 路径规则

- **禁止硬编码路径**，如 `C:\Users\EDY\...`
- 前端需要用户主目录时，用相对路径或调用后端接口获取
- 脚本用 `%~dp0`（Windows）或 `$(dirname "$0")`（Mac/Linux）获取脚本所在目录
- Node.js 脚本用 `os.homedir()`、`os.tmpdir()`、`__dirname`

### 5.2 代码规范

- 根据技术栈选择合适的代码规范，写在 PRODUCT.md 或单独的开发规范文件里
- 后端所有 API 以 `/api/` 开头
- 数据库操作后必须保存
- 时间格式：`YYYY-MM-DD HH:mm`
- 错误返回统一的 JSON 格式：`{ error: "错误信息" }`

### 5.3 Git 提交规则

- 不要每次修改都自动 push，由用户决定何时提交
- commit message 用中文，格式：`类型: 说明`
  - `feat: 新增功能`
  - `fix: 修复问题`
  - `docs: 文档更新`
  - `refactor: 重构代码`
  - `style: 样式调整`
- 详细流程见 `规则/Git同步规则.md`

### 5.4 命令输出

任何可能输出很多内容的命令，限制输出长度：

```bash
COMMAND 2>&1 | head -c 4000
```

---

## 六、高风险操作

以下操作必须先确认再做：

| 操作 | 风险 | 规则 |
|------|------|------|
| 改技术选型 | 影响全局 | 给方案让用户选，不替用户定 |
| 新增/删除功能 | 可能跑偏 | 先确认需求再动手 |
| 装软件/升级环境 | 改用户系统 | 先说明要装什么、为什么，用户同意再装 |
| 删文件 | 不可逆 | 先列清单让用户确认 |
| 写代码 | 可能白干 | 需求/设计确认后再写 |

---

## 七、AI 自检清单

AI 在开发中会反复犯同类问题。自检清单独立维护在 `AI自检清单.md`（项目根目录）。

- **开工前**：准备搭建新功能或改现有功能时，先读一遍清单
- **完成后**：功能做完准备交付时，对照清单逐项自检
- **发现新问题**：修复后追加到 `AI自检清单.md` 对应分类下

---

## 八、规则文件职责边界

写规则或规范时，先对照下表确认该写哪个文件：

| 文件 | 职责 | 写什么 | 不写什么 |
|------|------|--------|---------|
| `AGENTS.md` | 开发规则 | 目录结构、路径规则、代码规范、Git提交规则、高风险操作、规则文件职责边界 | 产品功能定义、视觉规范、具体业务规则 |
| `PRODUCT.md` | 产品说明 | 产品定位、目标用户、功能范围、做/不做、成功标准 | 代码怎么写、目录怎么组织、视觉样式 |
| `DESIGN.md` | 视觉规范 | 整体布局、配色方案、字号、组件样式、交互原则 | 产品功能、开发规则 |
| `规则/*.md` | 具体场景操作规范 | 文件组织、Git同步、新电脑初始化等 | 通用开发规则（写AGENTS.md）|
| `AI自检清单.md` | AI犯错清单 | 错误表现、根因、检查动作、首次发现日期 | 新规则（新规则写对应文件，清单只记已犯的错误）|

**判断方法**：
- 这条规则是"产品该有什么功能/字段" → PRODUCT.md
- 这条规则是"代码怎么写/文件怎么组织" → AGENTS.md
- 这条规则是"页面长什么样/用什么颜色" → DESIGN.md
- 这条规则是某个具体场景的操作规范 → 规则/目录下对应文件
- 这条规则是"AI又犯了什么错" → AI自检清单.md

---

## 九、文档变更记录

| 日期 | 变更 |
|------|------|
| 【填写日期】 | 初版，基于 project-base 地基包创建 |
