# AGENTS.md — 【项目名】开发规则

> 本文件是 AI 开发本项目时的必读规则。每次开工前先读，每次动手前先对照。
> 创建日期：【填写日期】

---

## 一、项目概述

【填写项目描述：这个项目是做什么的，一两句话说清】

**技术栈：**

【填写技术栈：前端/后端/数据库用了什么】

---

<!-- source: project-base -->
## 二、确认规则

> 确认是手段不是目的。规则只拦真要拦的，中间步骤自己推进，不要层层走流程。
> 规则管方向和底线，不管创意和判断。AI 在创意、判断、角度上要主动发挥，不要被规则框死。

### 2.1 需求未确认前，禁止写代码

用户问问题或提想法 ≠ 用户确认要做。用户可能在了解信息后改变主意。

- 用户提出需求或想法 → 先回答问题、给方案、列选项
- 用户明确说"做吧"/"开始吧"/"就这样" → 才动手
- 用户还在问、还在纠结、还在选 → 继续讨论，不动手

### 2.2 技术选型给方案让用户选，不替用户定

用户说"你看着办"不等于 AI 可以自己加约束（如"要和某项目同栈"其实是 AI 自己加的）。

- 给 2-3 个方案，说明每个的利弊
- 告诉用户"你不用懂技术，只需选方向"
- 不要自己加约束，约束来自用户需求，不来自 AI 的假设
- 用户选完后确认一遍"你选了X，理由是Y，对吗"
<!-- /source: project-base -->

---

## 三、开工前必读

每次开始工作时，先读：

```
AGENTS.md          ← 本文件，开发规则
操作手册.md         ← 初始化流程（仅首次开工读，引导用户完成初始化）
提示词手册.md       ← 用户用的提示词速查（用户问"说什么能干什么"时指向此文件）
PRODUCT.md         ← 产品目标和功能范围
DESIGN.md          ← 视觉风格和交互规范（如有UI）
AI自检清单.md       ← AI 反复犯的问题清单
规则/               ← 所有规则文件，新增/修改文件前必读《文件组织规则》，跨项目访问必读《项目索引规则》，按需读其他
```

**首次开工特别说明**：如果是 clone 地基包后的第一次开工，必须先读 `操作手册.md`，按流程引导用户完成初始化（填项目信息、配 Git、配 Agent 和 Skill 目录），完成后再开始干活。

读完以后，先用 3 句话说明：
- 当前项目是做什么的
- 当前做到哪一步
- 本次准备做什么（**等用户确认后再动手**）

---

## 四、目录结构

```
【项目名】/
├── AGENTS.md               # 开发规则（AI 必读）
├── 操作手册.md              # 初始化操作流程（AI 读这个引导用户完成初始化）
├── 提示词手册.md            # 对AI说什么能干什么（用户日常翻阅）
├── AI自检清单.md            # AI 反复犯的问题清单
├── PRODUCT.md              # 产品说明
├── DESIGN.md               # 设计规范（如有UI）
├── agents/                 # 开发协作 Agent（10个）
├── scripts/                # 自动化任务脚本模板（sync/auto-summary/plist）
├── 规则/                    # 具体场景操作规范
│   ├── 文件组织规则.md
│   ├── 项目索引规则.md
│   ├── Git同步规则.md
│   ├── 自动化任务规则.md
│   ├── Skill管理规则.md
│   └── 新电脑初始化.md
├── 产出/                    # AI和系统自动生成（不手动编辑）
│   ├── 每日总结/            # AI分析对话生成
│   ├── 决策记录/            # 决策过程沉淀（AI主动记）
│   ├── 任务日志/            # 定时任务执行日志
│   └── 测试报告/            # 全量测试报告
└── （根据项目需要新增目录）
```

---

## 五、开发规则

### 5.1 路径规则

- **禁止硬编码路径**，如 `C:\Users\EDY\...`
- 前端需要用户主目录时，用相对路径或调用后端接口获取
- 脚本用 `%~dp0`（Windows）或 `$(dirname "$0")`（Mac/Linux）获取脚本所在目录
- Node.js 脚本用 `os.homedir()`、`os.tmpdir()`、`__dirname`

### 5.2 代码规范

- 根据技术栈选择合适的代码规范，写在 PRODUCT.md 或单独的开发规范文件里
- 后端所有 API 以 `/api/` 开头
- 数据库操作后必须保存
- 时间格式：`YYYY-MM-DD HH:mm`
- 错误返回统一的 JSON 格式：`{ error: "错误信息" }`

### 5.3 Git 提交规则

- 不要每次修改都自动 push，由用户决定何时提交
- commit message 用中文，格式：`类型: 说明`
  - `feat: 新增功能`
  - `fix: 修复问题`
  - `docs: 文档更新`
  - `refactor: 重构代码`
  - `style: 样式调整`
- 详细流程见 `规则/Git同步规则.md`

### 5.4 命令输出

任何可能输出很多内容的命令，限制输出长度：

```bash
COMMAND 2>&1 | head -c 4000
```

---

## 六、高风险操作

以下操作必须先确认再做：

| 操作 | 风险 | 规则 |
|------|------|------|
| 改技术选型 | 影响全局 | 给方案让用户选，不替用户定 |
| 新增/删除功能 | 可能跑偏 | 先确认需求再动手 |
| 装软件/升级环境 | 改用户系统 | 先说明要装什么、为什么，用户同意再装 |
| 删文件 | 不可逆 | 先列清单让用户确认 |
| 写代码 | 可能白干 | 需求/设计确认后再写 |

---

## 七、AI 自检清单

AI 在开发中会反复犯同类问题。自检清单独立维护在 `AI自检清单.md`（项目根目录）。

- **开工前**：准备搭建新功能或改现有功能时，先读一遍清单
- **完成后**：功能做完准备交付时，对照清单逐项自检
- **发现新问题**：修复后追加到 `AI自检清单.md` 对应分类下

---

## 八、规则文件职责边界

写规则或规范时，先对照下表确认该写哪个文件：

| 文件 | 职责 | 写什么 | 不写什么 |
|------|------|--------|---------|
| `AGENTS.md` | 开发规则 | 目录结构、路径规则、代码规范、Git提交规则、高风险操作、规则文件职责边界 | 产品功能定义、视觉规范、具体业务规则 |
| `PRODUCT.md` | 产品说明 | 产品定位、目标用户、功能范围、做/不做、成功标准 | 代码怎么写、目录怎么组织、视觉样式 |
| `DESIGN.md` | 视觉规范 | 整体布局、配色方案、字号、组件样式、交互原则 | 产品功能、开发规则 |
| `规则/*.md` | 具体场景操作规范 | 文件组织、项目索引、Git同步、自动化任务、Skill管理、新电脑初始化等 | 通用开发规则（写AGENTS.md）|
| `AI自检清单.md` | AI犯错清单 | 错误表现、根因、检查动作、首次发现日期 | 新规则（新规则写对应文件，清单只记已犯的错误）|

**判断方法**：
- 这条规则是"产品该有什么功能/字段" → PRODUCT.md
- 这条规则是"代码怎么写/文件怎么组织" → AGENTS.md
- 这条规则是"页面长什么样/用什么颜色" → DESIGN.md
- 这条规则是某个具体场景的操作规范 → 规则/目录下对应文件
- 这条规则是"AI又犯了什么错" → AI自检清单.md

**新增/修改规则文件后**：按 `操作手册.md` 的"维护手册"章节的同步清单逐项检查，确保所有引用点都更新。改完触发规则测试员验证。

---

## 九、文档变更记录

| 日期 | 变更 |
|------|------|
| 【填写日期】 | 初版，基于 project-base 地基包创建 |
| 2026-08-03 | 新增《项目索引规则》；"先确认再动手"7条砍为2条精华版（2.3/2.7 删除，2.5/2.6 移入自检清单） |
| 2026-08-04 | Git同步规则新增"用户说'提交'是什么意思"小节；新增《提示词手册.md》；开工前必读清单补提示词手册引用；操作手册新增"维护手册"章节；rule-tester 加维护清单同步检查项；新增 scripts/ 目录（sync/auto-summary 脚本模板 + plist 模板 + README）；自动化任务规则/新电脑初始化 改为引用 scripts/README.md |
| 2026-08-06 | 新增 tech-director 技术总监 agent，建立双总监制衡机制；改造 project-director 职责收窄到需求+流程+业务；5个 agent 流程图同步更新；4个文档同步 8→9 个 agent |
